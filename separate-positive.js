"use strict";
// add whatever parameters you deem necessary & write docstring

/** Takes in an array of non-zero numbers
 *
 * Returns the same array with negative numbers on the left
 * and positive numbers on the right.
 */

function separatePositive() {
  let a = [1, 2, -4, 7, -9];
  // [1, 2, 7, -4, -9]
  a[2] =

  a[1] = 1;
}
